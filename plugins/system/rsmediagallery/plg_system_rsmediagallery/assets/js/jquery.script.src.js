function rsmg_init_system_equal_size($) {	var max = 0;	var current_items = [];	if ($('.rsmg_system_container').length == 0) return;	var old_top = $('.rsmg_system_container').offset().top;	var last	= $('.rsmg_system_container').length - 1;	$('.rsmg_system_container').each(function(index, el) {		var top = $(el).offset().top;		if (old_top != top)		{			for (var i=0; i<current_items.length; i++)				if (max > 0)					current_items[i].css('height', max);						top 	= $(el).offset().top;			old_top = top;			max 	= 0;			current_items.length = 0;		}				max = Math.max(max, $(el).height());		current_items.push($(el));				if (index == last)		{			for (var i=0; i<current_items.length; i++)				if (max > 0)					current_items[i].css('height', max);		}	});}jQuery(document).ready(function($) {	$("ul.rsmg_system_gallery li img").hover(		function () { $(this).stop().animate({ opacity: 0.7 }, "slow"); },		function () { $(this).stop().animate({ opacity: 1.0 }, "slow"); }	);	$(document).piroBox_ext({		piro_speed: 700,		bg_alpha: 0.5,		piro_scroll: true,		htmlClass: 'system',		selector: 'a[class*="pirobox_gall_system"]'	});	rsmg_init_system_equal_size(jQuery);});